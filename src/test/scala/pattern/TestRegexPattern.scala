/*
package pattern

import org.scalatest.matchers.should
import org.scalatest.wordspec.AnyWordSpec

class TestRegexPattern extends AnyWordSpec with should.Matchers {

  "A numeric regex" should {
    val regex = "[0-9]+".r
    "math" in {
      val result = regex.findAllIn(Array("abcd", "0935", "ab76").mkString)
      result.map(println)
    }
  }
}
*/
